#include "MainApp.h"

/*
    what to configure in config file:

        block_size (block_manager.h)
        cache_capacity (cache.h)
*/

int main() {
    
    MainApp app;
    app.run();

    return 0;
}
